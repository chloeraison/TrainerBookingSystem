@page
@model ClientsModel
@{
    ViewData["Title"] = "Clients";
}

<section>
    <h1>👥 Clients</h1>

    <a class="btn mb-3" asp-page="/Clients/Add">➕ Add New Client</a>

    <div class="table-responsive">
        <table class="table client-table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>🏋️ Gym</th>
                    <th>🕒 Pref. Time</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var c in Model.Clients)
            {
                <tr>
                    <td>
                        <a class="link" asp-page="/Details" asp-route-id="@c.Id">
                            @c.Name
                        </a>
                    </td>
                    <td>@c.Gym</td>
                    <td>@c.PreferredTimes</td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</section>

